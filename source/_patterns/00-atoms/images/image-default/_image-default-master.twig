{% set image_default_classes = image_default_classes|default() %}
{% set image_id = image_id|default('') %}
{% set image_src = image_src|default('fpo_4x3.png') %}
{% set image_alt = image_alt|default('') %}

<img id="{{ image_id }}" class="image-pl image--default {{ image_default_classes }}" src="{{ path_to_assets }}assets/{{ image_src }}" alt="{{ image_alt }}">